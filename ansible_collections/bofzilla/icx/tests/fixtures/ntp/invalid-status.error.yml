---
params:
  servers: []
  enabled: true
  save: false

output:
  - |-
    address            ref                clock    st    when    poll    reach    delay
    * synced, # selected, + candidate, - outlayer, x falseticker, ~ configured
  - "unexpected ntp status output"

error: "ValueError: raw switch output: unexpected ntp status output"
